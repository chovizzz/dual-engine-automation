# 中文参考文档 / Chinese Reference

## 工作区继承规则

**重要**: 本 Skill 在**调用它的 Agent 的工作区**中运行。

| 调用 Agent | 工作区路径 | 文件创建位置 |
|-----------|-----------|-------------|
| Main Agent | 当前 Agent 的工作区 | `memory/`、`HEARTBEAT.md` 等 |
| Work Agent (Discord) | `/Users/xxx/workspace-javis-discord/` | 相对于该工作区 |

**路径规则**:
- ✅ 使用**相对路径**: `memory/cron-execution-state.json`
- ✅ 使用**工作区相对路径**: `HEARTBEAT.md`、`SOUL.md`
- ❌ 不要使用绝对路径如 `/Users/xxx/workspace/...`

本 Skill 根据调用它的 Agent 自动将所有文件放置在正确位置。

## 快速开始

### 自然语言用法（推荐）

**系统监控：**
```
帮我搭个系统监控：
- 每30分钟检查CPU、内存、磁盘
- 每天9点生成健康报告
- CPU超过80%时告警
- 通知发到飞书 chat:oc_xxx
```

**Reddit运营：**
```
我要做Reddit运营：
- 每30分钟扫描r/TechSEO和r/GenEngineOptimization
- 早上9点生成报告，晚上10点自动发帖
- 飞书通知：chat:oc_ac50ef0887f1d1750a94a5aaecfdb959
```

**竞品监控：**
```
监控竞品价格：
- 每30分钟检查竞品网站价格变化
- 每周一9点生成周报
- 价格变动超过10%立即告警
```

### 配置型用法（高级）

```
监控需求：
- 每30分钟检查：CPU、内存、磁盘
- 告警阈值：CPU>80%

定时任务：
- 每天09:00：生成日报
- 每周一09:00：生成周报

通知设置：
- 飞书：chat:oc_xxx
```

## 默认文件结构

Skill自动在调用Agent的工作区创建：

```
workspace/                    # 调用Agent的工作区（自动识别）
├── HEARTBEAT.md              # HEARTBEAT指令
├── memory/                   # 数据存储
│   ├── cron-execution-state.json    # 状态管理
│   ├── content-calendar.md          # 内容日历
│   ├── content-ideas.md             # 内容创意库
│   ├── post-performance-log.md      # 表现追踪
│   ├── YYYY-MM-DD.md                # 日报
│   ├── performance-logs/            # 执行数据
│   ├── optimization-reports/        # 优化记录
│   └── learning/                    # 学习归档
│       ├── daily-reports/           # 日报汇总
│       ├── insights/                # 策略洞察
│       ├── hot-posts/               # 热点归档
│       └── competitors/             # 竞品情报
├── SOUL.md                   # Agent人设（可选）
├── AGENTS.md                 # 运营指南（可选）
└── OPTIMIZATION.md           # 优化记录（可选）
```

### 关键要点

✅ **自动放置**: 所有文件自动创建在标准目录
✅ **无需手动路径**: 不用指定路径，Skill自动处理
✅ **相对路径**: 所有命令中使用 `memory/xxx.md`
✅ **工作区继承**: 文件进入调用Agent的工作区

### 多Agent设置

使用多Agent路由时：

| Agent | 文件创建位置 | 示例 |
|-------|------------|------|
| Main Agent | 主工作区 | `workspace/memory/...` |
| Work Agent (Discord) | Discord工作区 | `workspace-javis-discord/memory/...` |

每个Agent在自己的工作区维护独立的文件结构。

## 自动翻译对照表

| 你说 | Skill理解 | 自动配置 |
|-----|----------|---------|
| "检查/扫描/监控 每30分钟" | HEARTBEAT任务 | 30分钟循环监控 |
| "每天XX点 / 定时" | CRON任务 | 定时执行 |
| "生成报告" | CRON深度任务 | 报告+通知 |
| "发布/发帖" | 内容任务 | 内容日历+发布 |
| "发到飞书" 或 "chat:oc_xxx" | 飞书通知 | channel=feishu |
| "不指定频道" | 默认频道 | 不加channel参数 |

## 飞书通知

### 获取Chat ID

在飞书群组中发送消息，OpenClaw会显示：
```json
{
  "channel": "feishu",
  "chat_id": "oc_ac50ef0887f1d1750a94a5aaecfdb959"
}
```

### 使用方式

**指定飞书：**
```
...通知飞书 chat:oc_ac50ef0887f1d1750a94a5aaecfdb959
```
→ 生成：`channel=feishu target=chat:oc_xxx`

**默认频道：**
```
...发送到当前会话
```
→ 使用：默认频道（不加`channel`参数）

### 通知规则

| 用户指定 | 目标 | 命令 | 使用场景 |
|---------|------|------|---------|
| 飞书 + chat:oc_xxx | 飞书群组 | `channel=feishu` | 团队协作 |
| 不指定频道 | 当前会话 | 无参数 | 个人测试 |

## 自进化机制

使用本Skill构建的系统默认**开启自进化**。

### 进化循环

```
执行 → 分析 → 决策 → 优化 → 更新 → 重新执行
 ↑                                    ↓
 └────────── 持续迭代 ←──────────────┘
```

### 自动优化

- **时间优化**: 自动调整最佳执行时间
- **内容优化**: 自动识别高表现内容
- **任务优化**: 自动拆分耗时任务
- **策略优化**: 自动更新策略文档

## 设计原则

1. **自然语言优先**: 使用日常语言，无需术语
2. **自动路径规划**: 文件自动放置在标准目录
3. **开箱即用**: 配置完成后即可运行
4. **自进化**: 自动分析、优化、迭代
5. **可扩展**: 通过对话添加/修改任务

## 完整示例对话

### 场景：Reddit运营

**用户：**
> 我要做Reddit运营，每30分钟扫描TechSEO和GEO板块，早上9点生成报告，晚上10点自动发帖

**Skill：**
> ✅ **已理解您的需求：**
> - HEARTBEAT: 每30分钟扫描r/TechSEO、r/GenEngineOptimization
> - CRON: 09:00日报，22:00发帖检查
> - 自进化: 已开启（自动优化）
>
> 📁 **已创建文件：**
> - HEARTBEAT.md（扫描指令）
> - memory/cron-execution-state.json（11个时段）
> - memory/content-calendar.md（发帖计划）
> - memory/learning/hot-posts/（归档）
> - SOUL.md（Reddit人设）
> - AGENTS.md（运营指南）
>
> ⏰ **CRON任务（8个）：**
> - 00:00 状态重置
> - 09:00 生成报告
> - 09:10 HEARTBEAT触发
> - 22:00 发帖执行
> - 20:00 Git自动提交
> - 20:30 每日数据分析
>
> 🚀 **系统搭建完成！**
> 下次执行：明天09:00
